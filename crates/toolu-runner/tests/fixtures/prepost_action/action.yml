name: 'prepost-fixture'
description: 'Real node action exercising pre/main/post + STATE_ cross-stage.'
inputs:
  marker:
    description: 'prefix written to MARKER_FILE so test can assert stage order'
    required: false
    default: 'X'
runs:
  using: 'node20'
  pre: 'pre.js'
  main: 'main.js'
  post: 'post.js'
