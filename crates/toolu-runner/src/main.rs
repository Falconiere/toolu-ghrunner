fn main() {
  // CLI is wired in step 7. For now, this is a placeholder so the binary
  // builds. The first end-to-end `cargo build --workspace` should succeed.
  println!("toolu-runner: not yet implemented — see docs/toolu/plans/");
}
