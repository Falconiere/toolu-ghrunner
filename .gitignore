docs/toolu
target/
**/*.rs.bk

# toolu-runner runtime state (per-user install)
config.toml
credentials.json
.lock
.pending_remove
_diag/

# tool session state — per-developer, never shared.
# .claude/settings.json is deliberately NOT ignored: it carries the guardrail
# hooks (CORE layer 2) that every agent working in this repo must inherit.
.claude/tmp/
.claude/toolu.config.json
.serena/

# Staged darwin/arm64 binary for the macOS carrier image (Dockerfile.macos).
dist/
