name: annotation-82-live
description: Issue 82 nested annotation probe.
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        printf '%s\n' '::warning file=.github/annotation-82-source.txt,line=11,title=Nested%3A first::composite one'
    - shell: bash
      run: |
        printf '%s\n' '::notice file=.github/annotation-82-source.txt,line=12,title=Nested%3A second::composite two'
